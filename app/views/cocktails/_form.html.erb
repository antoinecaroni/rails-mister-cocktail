<%= simple_form_for [@restaurant] do |f| %>
  <%= f.input :name %>
  <%= f.association :address %>
  <%= f.association :phone_number %>
  <%= f.submit "Save the Restaurant", class: "btn btn-primary" %>
<% end %>
